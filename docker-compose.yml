version: '3.8'
services:
  bot:
    build: .
    env_file: .env
    depends_on:
      - redis
    restart: always

  redis:
    image: redis:alpine
    restart: always
    volumes:
      - redis_data:/data

volumes:
  redis_data:
